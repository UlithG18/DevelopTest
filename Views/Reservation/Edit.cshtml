@model DevelopTest.Models.Reservation

<div class="page-header">
    <div>
        <h1 class="page-title">Edit Reservation</h1>
        <p class="page-subtitle">Update booking details</p>
    </div>
</div>

<div class="form-card">
    <form asp-action="Update" asp-controller="Reservation" method="post">
        <input type="hidden" asp-for="Id" />
        <div class="mb-3">
            <label class="form-label" asp-for="UserId">User</label>
            <select asp-for="UserId" asp-items="ViewBag.Users" class="form-control">
                <option value=""> Select a User </option>
            </select>
        </div>
        <div class="mb-3">
            <label class="form-label" asp-for="SportAreaId">Sport Area</label>
            <select asp-for="SportAreaId" asp-items="ViewBag.SportAreas" class="form-control">
                <option value=""> Select a Sport Area </option>
            </select>
        </div>
        <div class="mb-3">
            <label class="form-label" asp-for="Status">Status</label>
            <select asp-for="Status" class="form-control" asp-items="Html.GetEnumSelectList<ReservationStatus>()">
                <option value=""> Select a Status </option>
            </select>
        </div>
        <div class="mb-3">
            <label class="form-label" asp-for="Date">Date</label>
            <input asp-for="Date" type="date" class="form-control" />
        </div>
        <div class="mb-3">
            <label class="form-label" asp-for="StartTime">Start Time</label>
            <input asp-for="StartTime" type="time" class="form-control" />
        </div>
        <div class="mb-3">
            <label class="form-label" asp-for="EndTime">End Time</label>
            <input asp-for="EndTime" type="time" class="form-control" />
        </div>
        <div class="form-actions">
            <button type="submit" class="btn-primary-custom">Update Reservation</button>
            <a asp-action="Index" asp-controller="Reservation" class="btn-ghost-custom">Cancel</a>
        </div>
    </form>
</div>
